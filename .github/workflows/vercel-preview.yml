name: Vercel Preview Deployment

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read
  pull-requests: write

jobs:
  preview:
    name: Deploy Preview
    runs-on: ubuntu-latest

    env:
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Dependencies
        run: npm ci --no-audit --no-fund

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment
        if: github.event.pull_request.head.repo.full_name == github.repository
        run: |
          vercel pull \
            --yes \
            --environment=preview \
            --token=${{ secrets.VERCEL_TOKEN }}

      - name: Build Project
        if: github.event.pull_request.head.repo.full_name == github.repository
        run: |
          vercel build \
            --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy Preview
        if: github.event.pull_request.head.repo.full_name == github.repository
        id: deploy
        run: |
          url=$(vercel deploy --prebuilt --token=${{ secrets.VERCEL_TOKEN }})
          echo "preview_url=$url" >> "$GITHUB_OUTPUT"

      - name: Update PR Comment
        if: github.event.pull_request.head.repo.full_name == github.repository
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: vercel-preview
          message: |
            ## 🚀 Preview Deployment Ready

            **Preview URL**

            ${{ steps.deploy.outputs.preview_url }}

            ---
            ### Build Status
            - ✅ Build completed successfully
            - 🔄 This comment updates automatically whenever new commits are pushed.

      - name: Comment on Fork PR
        if: github.event.pull_request.head.repo.full_name != github.repository
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: vercel-preview
          message: |
            ## ℹ️ Fork PR Notice

            This is a pull request from a forked repository. Vercel preview deployments are skipped for security reasons.
